---
// LANDING (EN) - /en/houston/ai-seo - "SEO for AI / AI Visibility" (AEO).
// The biggest wedge: getting ChatGPT/Gemini/Meta AI to RECOMMEND YOU. Bespoke page
// (services pattern) that reuses the DS components + PriceGrid for the 3 tiers
// (free diagnostic - foundations $500 - monitoring $200/mo).
// Schema: Service with Offer x3 (ServiceLd with tiers) + FAQPage (Faq) +
// BreadcrumbList (Breadcrumb). EN mirror of /es/houston/seo-para-ia.
import Layout from '../../../layouts/Layout.astro';
import SiteNav from '../../../components/layout/SiteNav.astro';
import SiteFooter from '../../../components/layout/SiteFooter.astro';
import Breadcrumb from '../../../components/ui/Breadcrumb.astro';
import LandingHero from '../../../components/sections/LandingHero.astro';
import AnswerBlock from '../../../components/sections/AnswerBlock.astro';
import Prose from '../../../components/sections/Prose.astro';
import PriceGrid from '../../../components/sections/PriceGrid.astro';
import FeatureGrid from '../../../components/sections/FeatureGrid.astro';
import Faq from '../../../components/sections/Faq.astro';
import CtaBand from '../../../components/sections/CtaBand.astro';
import Contact from '../../../components/sections/Contact.astro';
import ServiceLd from '../../../components/seo/ServiceLd.astro';
import RelatedLinks from '../../../components/sections/RelatedLinks.astro';
import { content } from '../../../i18n/content';
import { clusterNavEn } from '../../../i18n/clusters';
import { seoIaPage as p, aeoTiers } from '../../../i18n/seo-ia.en';

const c = content.en;
---
<Layout
  title={p.meta.title}
  description={p.meta.description}
  lang="en"
  path="houston/ai-seo"
>
  <ServiceLd slot="head" {...p.service} />

  <SiteNav nav={clusterNavEn} lang="en" homeHref="/en/" currentPath={Astro.url.pathname} />

  <main id="main">
    <LandingHero
      badge={p.hero.badge}
      badgeIcon={p.hero.badgeIcon}
      kicker={p.hero.kicker}
      h1={p.hero.h1}
      sub={p.hero.sub}
      primary={p.hero.primary}
      secondary={p.hero.secondary}
      chips={p.hero.chips}
      tone={p.hero.tone}
    >
      <Breadcrumb slot="breadcrumb" items={p.breadcrumb} lang="en" />
    </LandingHero>

    <AnswerBlock lang="en" q={p.answer.q} a={p.answer.a} source={p.answer.source} />

    <Prose
      tag={p.distinction.tag}
      title={p.distinction.title}
      paragraphs={p.distinction.paragraphs}
      tone={p.distinction.tone}
    />

    <PriceGrid lang="en" tag={p.grid.tag} title={p.grid.title} intro={p.grid.intro} items={aeoTiers} />

    <FeatureGrid tag={p.mechanics.tag} title={p.mechanics.title} items={p.mechanics.items} tone={p.mechanics.tone} />

    <Prose
      tag={p.caso0.tag}
      title={p.caso0.title}
      paragraphs={p.caso0.paragraphs}
      tone={p.caso0.tone}
    />

    <Faq tag={p.faq.tag} title={p.faq.title} items={p.faq.items} />

    <CtaBand lang="en"
      title={p.cta.title}
      sub={p.cta.sub}
      primary={p.cta.primary}
      secondary={p.cta.secondary}
      tone={p.cta.tone}
    />

    <RelatedLinks tag={p.related.tag} title={p.related.title} links={p.related.links} />

    <Contact contact={c.contact} defaultCity="Houston, TX" defaultService="SEO for AI (AI Visibility)" />
  </main>

  <SiteFooter lang="en" footer={c.footer} />
</Layout>
